Every app should ensure that a client can only access resources that they own or have permissions to access. When you access and/or modify things (objects) that you shouldn't, you're making an **I**nsecure **D**irect **O**bject **R**eference.

As a specific example of this bug on Discord bots; sending messages to channels where you can't write using the Dashboard.