The **C**ommon **V**ulnerabilities and **E**xposures, is a program that records vulnerabilities across popular apps and libraries. They assign it an ID of the form `CVE-YYYY-XXXXX`, where `YYYY` is the disclosure year and `XXXXX` a random number.

Here you can see apps that were using outdated software, and had CVEs.